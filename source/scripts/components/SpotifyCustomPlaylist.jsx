import React, { PropTypes } from 'react';
import ReactDOM from 'react-dom';

class SpotifyCustomPlaylist extends React.Component {
    componentDidMount() {
    }

    render() {
        return <div></div>
    }
}

SpotifyCustomPlaylist.propTypes = {
};

SpotifyCustomPlaylist.defaultProps = {
};

export default SpotifyCustomPlaylist;
